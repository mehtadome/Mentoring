# Does this code work?
```
arr  = [1, 2, 3, 4]
arr2 = [1, 2, 3, 4, 5, 6]
i = 0
 while (i < len(arr2)):
    print (arr[i], arr2[i])
```
Ans:

# Is this code syntatically correct?:
```
arr = [1 , 2 , 3 , 4 , 5 , 6]
```
Ans [Y/N]:

# Is this code syntatically correct?:
```
arr = [1 , 2 , 3 , 4 , 5 , 6]
```
Ans [Y/N]:

# What is the output of the following?:
```
import math
arr = [0, 1, -1, 1, 0]
for i in arr:
    if i < 0:
        abs(i)
print (arr)
```
Ans:

# What is the output of the following?:
```
arr = []
for i in range(len(arr)):
    if arr[i] is None:
        arr[i] = i + 1
print(arr)
```
Ans:

# What is the output of the following?:
```
import math
arr = [0, 1, -1, 1, 0]
for i in arr:
    if i < 0:
        i = abs(i)
print (arr)
```
Ans:


